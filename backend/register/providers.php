<?php namespace register; trait providers { static function getProviders() { return [

    /*
    |
    | List of the providers for register. The providers placed in vendor directory.
    | But, not all providers can be registered.
    |
    */
    'PhpFastCache'

];}}
